import { reactive } from "vue";

export default reactive({
  theme: {
    bgColor: "#ffffff",
    txColor: "#808080",
    hdColor: "#808080",
    snColor: "#808080",
    avtarRingColor: "rgb(74 222 128 / 1)"
  },
});
